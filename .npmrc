# npm workspaces (npm >= 10, the version shipped with Node 22). No Corepack,
# no other package manager: npm is the only one this repository supports.
# Keep this file small: anything here applies to every `npm install`/`npm ci`,
# including the ones inside infra/docker/*.Dockerfile.
fund=false
audit=false
