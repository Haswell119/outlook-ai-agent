apiVersion: v2
name: outlook-ai-orchestrator
description: Outlook AI Orchestrator — add-in (nginx/TLS), orchestrator API + worker, admin dashboard, optional in-cluster PostgreSQL/pgvector. Production packaging for Nutanix Kubernetes Platform (NKP).
type: application

# Chart version: bumped by CI (release.yml) from the git tag.
version: 0.1.0
# Application version: MUST match the root package.json "version" field.
appVersion: "0.1.0"

kubeVersion: ">=1.27.0-0"

home: https://github.com/northbridge-capital/outlook-ai-agent
sources:
  - https://github.com/northbridge-capital/outlook-ai-agent

keywords:
  - outlook
  - office-addin
  - llm
  - human-in-the-loop
  - nkp

maintainers:
  - name: Northbridge Capital — Platform Team
    email: platform@northbridge.example

annotations:
  category: ProductivityAndCollaboration
  licenses: Proprietary
